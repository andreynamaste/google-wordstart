# 🚀 Быстрый старт Google Word Start

## Что создано

✅ Flask приложение (`app.py`) - backend для работы с Google Keyword Planner  
✅ Frontend интерфейс (`templates/index.html`) - веб-интерфейс приложения  
✅ WordPress страница (`wordpress-page.html`) - для вставки на сайт  
✅ WordPress карточка (`wordpress-card.html`) - для главной страницы thamini  
✅ Конфигурация Make.com - все необходимые JSONC файлы  
✅ Инструкции по развертыванию  

## 📍 Структура файлов

```
Гугл ворд старт/
├── app.py                          # Flask приложение
├── requirements.txt                # Зависимости Python
├── templates/
│   └── index.html                 # Frontend интерфейс
├── wordpress-page.html            # Страница для WordPress
├── wordpress-card.html            # Карточка для главной страницы
├── .env.example                   # Пример переменных окружения
├── README.md                      # Полная документация
├── DEPLOYMENT.md                  # Инструкция по развертыванию
└── Конфигурация Make.com:
    ├── google-wordstart-base.jsonc
    ├── google-wordstart-connection-parameters.jsonc
    ├── google-wordstart-mappable-parameters.jsonc
    ├── google-wordstart-interface.jsonc
    ├── google-wordstart-communication.jsonc
    └── google-wordstart-connection-communication.jsonc
```

## ⚡ Быстрая установка

### 1. Установите зависимости

```bash
cd "/root/андрей/жена/Гугл ворд старт"
pip3 install -r requirements.txt
```

### 2. Настройте переменные окружения

```bash
cp .env.example .env
nano .env  # Заполните ваши API ключи
```

### 3. Запустите приложение

```bash
python3 app.py
```

Приложение будет доступно на `http://localhost:5002`

## 🌐 Интеграция с WordPress

### Шаг 1: Добавьте карточку на главную страницу

1. Откройте главную страницу `https://2msp.online/thamini/` для редактирования
2. Вставьте содержимое из `wordpress-card.html`

### Шаг 2: Создайте страницу wordstart

1. В WordPress создайте новую страницу с slug `wordstart`
2. Вставьте содержимое из `wordpress-page.html` в редактор (HTML режим)
3. Опубликуйте страницу

### Шаг 3: Настройте проксирование (если нужно)

Если приложение запущено на другом порту, настройте Nginx:

```nginx
location /thamini/wordstart {
    proxy_pass http://localhost:5002;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
}
```

## 🔗 Ссылки

- **Главная страница**: https://2msp.online/thamini/
- **Приложение**: https://2msp.online/thamini/wordstart
- **API Health**: https://2msp.online/thamini/wordstart/health

## 📝 Важно

⚠️ **Текущая версия использует моковые данные** для демонстрации.  
Для работы с реальным Google Ads API необходимо:

1. Зарегистрироваться в Google Ads
2. Создать OAuth приложение
3. Получить Client ID и Client Secret
4. Настроить токены доступа
5. Обновить код в `app.py` для работы с реальным API

## 🆘 Помощь

Подробные инструкции смотрите в:
- `README.md` - полная документация
- `DEPLOYMENT.md` - инструкция по развертыванию на сервере

